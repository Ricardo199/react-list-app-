# React List App

In the project directory, you can run:

### `npm start`

Runs the app in development mode with Vite.

### `npm run build`

Builds the app for production to the `dist` folder.

### `npm test`

Prints a message and exits successfully (no automated tests are currently configured).
